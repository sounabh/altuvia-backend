{
  "name": "altuvia-backend",
  "version": "1.0.0",
  "description": "backend work for altuvia website",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:sync": "npm run prisma:generate && npm run prisma:push",
    "deploy": "npm run prisma:generate && npm run prisma:deploy && npm start",
    "build": "npm run prisma:generate",
    "postinstall": "chmod +x node_modules/.bin/prisma || true && npx prisma generate"

  },
  "dependencies": {
    "@prisma/client": "^6.10.0",
    "prisma": "^6.13.0",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.1",
    "helmet": "^8.1.0",
    "joi": "^17.13.3",
    "jsonwebtoken": "^9.0.2",
    "qrcode": "^1.5.4",
    "speakeasy": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
